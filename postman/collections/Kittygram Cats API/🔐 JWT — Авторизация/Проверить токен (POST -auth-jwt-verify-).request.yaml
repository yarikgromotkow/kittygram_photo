$kind: http-request
name: Проверить токен (POST /auth/jwt/verify/)
url: "{{base_url}}/auth/jwt/verify/"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "token": "{{token}}"
    }
order: 3000
